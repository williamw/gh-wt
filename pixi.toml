[workspace]
authors = ["Bill Welense"]
channels = ["conda-forge"]
name = "gh-wt"
platforms = ["osx-arm64"]
version = "0.1.0"

[tasks]
test = "python -m pytest test_gh_wt.py -v"

[dependencies]
python = ">=3.10,<4"
pytest = ">=7.0"
pyright = ">=1.1"
